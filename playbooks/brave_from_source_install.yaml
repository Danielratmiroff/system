---
- name: Installing Brave explorer from source
  hosts: all
  become: false
  gather_facts: false

  tasks:
    - name: Installing brave via install sh script
      shell: curl -fsS https://dl.brave.com/install.sh | sh
